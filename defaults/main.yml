---
# defaults file for kvm-host
kvm_host_apt_packages:
  - kvm
  - libvirt
  - libvirt-clients
  - qemu
  - vagrant
  - vagrant-libvirt

# List of user accounts to be added to the 'libvirt' group,
# which permits non-sudoized management of virtual machines.
kvm_host_users: []
